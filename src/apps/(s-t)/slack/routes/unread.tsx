import React, { FC } from "react";
import { View, Text } from "react-native";

type Props = {};

export const Unread: FC<Props> = ({}) => {
  return (
    <View>
      <Text>Unread</Text>
    </View>
  );
};
