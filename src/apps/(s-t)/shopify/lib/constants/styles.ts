// shopify-tabs-shared-header-animation 🔽

/**
 * Fixed heights used by the shared tabs header stack.
 * Why: These "magic numbers" keep vertical rhythm consistent and allow other
 * components to calculate layout/animations (e.g., sticky header, reveal timing)
 * without measuring at runtime.
 */
export const SEARCH_BAR_HEIGHT = 42;
export const SEARCH_FILTERS_HEIGHT = 56;

// shopify-tabs-shared-header-animation 🔼
