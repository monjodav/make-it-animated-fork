import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const Menu = () => {
  return (
    <View>
      <Text>Menu</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
