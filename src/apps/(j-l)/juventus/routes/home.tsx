import { View } from "react-native";
import Calendar from "../components";

const Home = () => {
  return (
    <View className="flex-1 bg-black pt-[200px]">
      <Calendar />
    </View>
  );
};

export default Home;
