// colorsapp-palette-picker-color-change-animation 🔽

// Sizing presets shared across the panel and sliders to keep proportions consistent.
// These impact both touch targets (accessibility) and animation readability (e.g. ring thickness).
export const sharedConfigs = {
  // Panel thumb diameter. Big enough to track during drag without covering too much spectrum.
  thumbPanelSize: 24,
  // Slider thumb diameter. Slightly smaller than panel to reduce visual weight.
  thumbSliderSize: 24,
  // Slider track thickness. Thinner rails read as "precise" and keep focus on the colors.
  sliderThickness: 4,
};

// colorsapp-palette-picker-color-change-animation 🔼
