User must provide the search key.

If no search key is provided, ask the user for the search key. (for example: "alma-nutrients-circular-carousel-animation" or "gmail-header-scroll-animation").

Find all files containing the provided search key: [slug] (Only if the search key is provided).

Output the list as "Detected files".